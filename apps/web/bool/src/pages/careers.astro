---
import { resolveImage } from '@bool/content';
import { t, tOptional, isVisible } from '@bool/i18n';
import BaseLayout from '@bool/ui/layout/BaseLayout.astro';
import CareersCtaSection from '@bool/ui/sections/CareersCtaSection.astro';
import CareersHeroSection from '@bool/ui/sections/CareersHeroSection.astro';
import { getHeaderLabels, getFooterLabels } from '../labels';

const bgCareersHero = tOptional('careers.backgroundImage');
const bgCareersCta = tOptional('careersCta.backgroundImage');

const showCareersHero = isVisible('careers.visible');
const showCareersCta = isVisible('careersCta.visible');
---

<BaseLayout
  title={t('pageMeta.careers.title')}
  description={t('pageMeta.careers.description')}
  headerLabels={getHeaderLabels()}
  footerLabels={getFooterLabels()}
>
  {
    showCareersHero && (
      <CareersHeroSection
        headingId="careers-heading"
        ariaLabel={t('careers.heading')}
        heading={t('careers.heading')}
        description={t('careers.description')}
        backgroundImage={bgCareersHero ? resolveImage(bgCareersHero) : undefined}
      />
    )
  }
  {
    showCareersCta && (
      <CareersCtaSection
        tag={t('careersCta.tag')}
        heading={t('careersCta.heading')}
        body={t('careersCta.body')}
        cta={t('careersCta.cta')}
        backgroundImage={bgCareersCta ? resolveImage(bgCareersCta) : undefined}
      />
    )
  }
</BaseLayout>
