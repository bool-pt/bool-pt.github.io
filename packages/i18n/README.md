# @bool/i18n

Flat-key translation system with `t()`, `tList()`, and `tCollection()` helpers. Locale JSON files are the source of truth for all section content. See [CLAUDE.md](../../.claude/CLAUDE.md).
