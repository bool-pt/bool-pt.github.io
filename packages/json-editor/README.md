# @bool/json-editor-core

Core logic for the JSON editor — parser, serializer, field-kind detection, and tree operations. UI lives in `apps/web/json-editor`. See [CLAUDE.md](../../.claude/CLAUDE.md).
